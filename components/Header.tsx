import styles from '@styles/Header.module.css'

export default function Header() {
  return (
    <header className={styles.container}>
      <h1 className={styles.title}>All about Next.js! ✨</h1>
    </header>
  );
}
